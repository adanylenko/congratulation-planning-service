{
  "name": "congratulation-planning-service-backend",
  "version": "1.0.0",
  "description": "Congratulation planning service",
  "main": "index.js",
  "scripts": {
    "test": "tsc && mocha build/**/*.spec.js",
    "dev": "ts-node-dev --respawn --transpileOnly ./src/index.ts",
    "prod": "tsc && node ./build/index.js",
    "build": "tsc",
    "deploy": "tsc && bash -x ./deploy.sh"
  },
  "author": "py3ir",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/mocha": "^5.2.6",
    "mocha": "^6.0.2",
    "ts-node-dev": "^1.0.0-pre.32",
    "typescript": "^3.4.1"
  },
  "dependencies": {
    "express": "^4.17.3"
  }
}
